---
description: Ask a non-blocking side question in an ephemeral background session
---

Ask this side question without interrupting the main task:

$ARGUMENTS

The global `btw` plugin handles this command asynchronously. Do not turn this into the main task.
